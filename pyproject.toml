[project]
name = "finance-context-builder"
version = "0.1.0"
description = "Parse Excel cash-flow models into JSON and Markdown context"
readme = "README.md"
license = "Apache-2.0"
requires-python = ">=3.12"
dependencies = [
    "duckdb>=1.1",
    "fastapi>=0.141.1",
    "httpx>=0.28.1",
    "lxml>=5.3",
    "numpy>=2.0",
    "openai>=1.0",
    "pydantic>=2.9",
    "pydantic-settings>=2.6",
    "python-multipart>=0.0.12",
    "pyyaml>=6.0",
    "scipy>=1.13",
    "typer>=0.16",
    "uvicorn>=0.32",
]

[dependency-groups]
dev = [
    "httpx>=0.28.1",
    "pytest>=8.3",
    "ruff>=0.8",
]

[project.scripts]
finance-context = "finance_context.cli:app"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/finance_context"]

[tool.hatch.build.targets.wheel.force-include]
"src/finance_context/ontology/taxonomy.yaml" = "finance_context/ontology/taxonomy.yaml"

[tool.uv]
default-groups = ["dev"]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]
addopts = "-q"
markers = [
    "live_llm: calls a real LLM or embedding endpoint",
    "e2e_models: runs against local real Excel workbooks",
]

[tool.ruff]
line-length = 100
target-version = "py312"
src = ["src", "tests"]

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B"]
